const fs=require('fs'),path=require('path'),defaults=require('../config/defaults');const dir=path.join(process.cwd(),'data'),file=path.join(dir,'settings.json');function init(){if(!fs.existsSync(dir))fs.mkdirSync(dir,{recursive:true});if(!fs.existsSync(file))fs.writeFileSync(file,JSON.stringify(defaults.settings,null,2))}function get(){init();try{return {...defaults.settings,...JSON.parse(fs.readFileSync(file))}}catch{return {...defaults.settings}}}function update(p){const n={...get(),...p};fs.writeFileSync(file,JSON.stringify(n,null,2));return n}module.exports={get,update};